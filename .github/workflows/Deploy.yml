name: PHP Composer

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  deploy-db:

    runs-on: ubuntu-latest

    steps:

    - name: echo
      run: echo "test"

